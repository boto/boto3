{
  "type": "api-change",
  "category": "``dsql``",
  "description": "[``botocore``] UpdateCluster now checks the RemovePeerCluster permission on the specific cluster being removed, not a wildcard and docs now clarify how to set kmsEncryptionKey so the cluster uses the AWS-owned key."
}
